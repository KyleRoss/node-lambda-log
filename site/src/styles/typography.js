import Typography from 'typography';

const typography = new Typography({
  baseFontSize: '16px',
  baseLineHeight: 1.5,
  headerFontFamily: ['Fira Sans', 'Helvetica', 'Arial', 'sans-serif'],
  headerWeight: '200',
  bodyFontFamily: ['Fira Sans', 'Helvetica', 'Arial', 'sans-serif'],
  bodyWeight: '400',
  scaleRatio: 4
});

export default typography;
